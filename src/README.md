This dir contains the decompiled C source code for Pokemon Black.

The source files are organized to match the structure of the original game.

* File names should be descriptive of their functionality
* Use lowercase with underscores for file names (e.g., `battle_system.c`)
* Header files go in the `include/` directory
